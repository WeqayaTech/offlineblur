# heavy / regenerated — never commit
out/
pod_archive/
weights/
videos/
*.tar.gz
*.mp4
*.webm
*.mov
*.mkv
__pycache__/
*.pyc
.DS_Store
.serve.pid
# owner-supplied source clips (copyright): keep out of the repo
ali-dawah-*.mp4

# v2 experimental pipeline — untested, kept out of the v1 release repo (owner to decide)
v2/
